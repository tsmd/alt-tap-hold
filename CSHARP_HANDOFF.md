# 左右 Alt tap-hold 常駐アプリ：C# 実装への引継ぎ

作成日：2026-09-22

## 1. この資料の目的と読み方

Windows 用の左右 Alt tap-hold ツールを **C# でフルスクラッチ実装する**。

以下の「必須要件」は、ユーザーと合意済みの現行仕様および維持すべき動作である。「推奨設計」は実装の参考である。

## 2. 目的と最重要要件

左 Alt の短い単独タップを **無変換**、右 Alt の短い単独タップを **変換** にする。長押しや他キーとの組み合わせでは、元の側の Alt 修飾キーとして使う。

対象アプリの中に、Alt を押した瞬間の `Alt down` に反応するものがある。そのため、次が最重要である。

> 短い単独タップでは、対象アプリへ Alt down を一切送らず、無変換／変換の down と up だけを送る。

物理 Alt をいったん通してからメニュー状態を消す方式は不可。Esc やダミーキーを補助的に送って帳尻を合わせる方式も不可。

## 3. 利用前提・対象範囲

| 項目 | 前提・要件 |
| --- | --- |
| OS | Windows。具体的な Windows バージョンは未指定。実装・検証したバージョンを成果物に記録する |
| 言語 | C#。使用する .NET バージョン・UI基盤・ビルド方式は実装者が選択して明記する |
| アーキテクチャ | x86／x64 の両方を扱える実装・ビルド手順とする。 |
| キーボード配列 | 日本語キーボード配列での利用を想定。右 Alt を AltGr として扱う配列は対象外 |
| 起動時 | ユーザーは全キーを離して起動する。起動時に全キーを走査して押下記録を作る処理は不要 |
| 文字キーの先押し | 文字キーを押したまま Alt を後押しすることはない。その操作を正確に組み合わせ判定する必要はない |
| Alt の保持時間 | ユーザーは Alt を5秒以上保持しない。5秒は異常な押下記録を捨てる期限として使える |
| 他のリマップ | 他の Alt リマップツールとの併用は前提にしない |
| マウス | タップ判定中のクリック・ホイールは組み合わせ判定の対象外。Alt+クリックは Alt が長押し判定された後に使う |
| 権限 | 通常のユーザーデスクトップを対象とする。管理者権限のアプリには同等の権限が必要になる場合がある |

x86 ビルド対応は、すべての旧32 bit Windows のサポートを意味しない。対象OSとランタイムの対応範囲は、採用する .NET に合わせて明記する。

## 4. 必須の入力動作

### 4.1 時間と対応キー

| 設定・キー | 値 |
| --- | --- |
| タップ判定時間 | **150 ms** |
| Alt 最大保持時間 | **5000 ms（5秒）** |
| 左 Alt | `VK_LMENU = 0xA4` |
| 右 Alt | `VK_RMENU = 0xA5` |
| 無変換 | `VK_NONCONVERT = 0x1D` |
| 変換 | `VK_CONVERT = 0x1C` |

150 ms 未満だけがタップ候補であり、**150 ms ちょうどは長押し**。5秒ちょうどで期限到達とする。200 ms は過去の設定であり、採用しない。

この2つの時間値は、少なくともソース内の明確な設定値として変更できるようにする。設定画面や設定ファイルは必須ではない。

無変換／変換のキーイベントを送ることが目的であり、IME の現在状態を取得して独自に ON/OFF を切り替える機能は要求しない。結果としてどの入力モードになるかはユーザーの IME 設定に依存する。

### 4.2 単独タップ

1. 左右どちらかの Alt down を捕捉し、物理イベントは Windows の通常のキー入力経路へ通さない。
2. この時点で、反対側 Alt が押されておらず、Shift・Ctrl・Win も押されていなければ、判定待ちにする。
3. 判定待ち中に別のキー down が発生せず、押下から150 ms未満で Alt up を受け取ったら、物理 Alt up も抑止する。
4. 左なら無変換 down/up、右なら変換 down/up をそれぞれ1回だけ送る。
5. Alt down/up、Esc、メニュー解除用キーなどは付け加えない。

### 4.3 長押し

- 判定待ちのまま150 msに到達したら、その側の Alt down を1回送信して、修飾キーとして有効にする。
- 5秒未満で物理 Alt up を受け取ったら、その側の Alt up を送って解放する。無変換／変換は送らない。
- 同じ押下中のリピート down によって、開始時刻を変更したり Alt down を繰り返し生成したりしない。
- タイマーが遅れ、Alt down をまだ送っていない状態で150 ms以上・5秒未満の Alt up が到着した場合も、タップにはしない。互換動作として Alt down/up を順に送り、その押下を終了する。
- 単独 Alt の長押しによってアプリのメニューが選択される等の通常動作は許容する。Esc で打ち消さない。

### 4.4 Alt より先に修飾キーが押されていた場合

- Alt の新規押下時に、**その時点の Shift・Ctrl・Win の状態だけ**を Windows に問い合わせる。
- 左右 Shift、左右 Ctrl、左右 Win のいずれも対象。左右の区別が不要な照会では、Shift と Ctrl はそれぞれ集約した状態を使ってよい。
- どれかが押されていたら、その Alt は最初から組み合わせ入力とし、150 msを待たず Alt down を送る。
- 短時間で Alt を離しても無変換／変換は送らない。
- 他の修飾キーを勝手に解放・再押下しない。
- **Alt 以外のキーの押下・解放履歴を保持する辞書、集合、一覧を作らない。** 先押し判定のために全キーを走査する必要もない。

### 4.5 Alt を先に押し、後から別のキーを押した場合

- Alt の判定待ち中に別のキー down が来たら、経過が150 ms未満でも即座に Alt 修飾へ移行する。
- 相手キーには文字、Tab、ファンクションキー、Shift・Ctrl・Win などを含む。
- **Alt down → 相手キー down の順序**を対象アプリ／Windows が観測できるようにする。
- 相手キーを再送する実装では、元の down を抑止し、二重入力やキーの欠落を防ぐ。
- Alt がすでに有効になった後の通常キーの down/up・リピートは、可能な限り通常どおり通す。
- Alt より先に相手キーを離しても、また相手キーより先に Alt を離しても、キーが残らない。
- 文字キーの押下履歴は保存しない。Alt 保持中に新しい他キー down を観測すれば十分である。

### 4.6 必須のショートカット

- 左 Alt+Tab、右 Alt+Tab。
- 左右いずれの Alt でも Alt+Shift+Tab。
- Alt を押してから150 ms以内に Tab を押す素早い操作。
- Alt 保持中の複数回の Tab、Tab のリピート、Shift を途中で押す／離す操作。
- Alt を離したときに通常どおり切り替えが確定すること。
- 通常のアプリの Alt ショートカット。例：Alt+F、Alt+F4。ただし実機テストでは閉じても支障のないテスト用ウィンドウを使う。

独自のウィンドウ切替画面で代替せず、Windows の通常の Alt+Tab を動作させる。Ctrl+Alt+Delete 等のセキュアアテンション操作を合成入力で実現する要求ではない。

### 4.7 左右 Alt の重なり

- 左右の押下時刻・判定状態・送信済み状態は独立して管理する。
- 片方の Alt を保持した状態で反対側を押したら、両側を修飾キーとして扱う。まだ判定待ちの側も有効化する。
- この組み合わせを無変換／変換のタップにはしない。
- 一方を離したら、その側だけを解放する。もう一方を勝手に解放しない。
- 押す順・離す順のいずれの組み合わせでも、両方を離した後の単独タップが正常に動くこと。
- **左右同時押しを特別なリセット操作にはしない。** 過去に案として出たが、現在は5秒制限による復旧を採用している。

## 5. 5秒制限と故障時の復旧

### 5.1 固定期限

- 各 Alt の最初の down から5000 msを期限とする。
- 他キー入力、Alt のリピート、反対側 Alt の押下によって期限を延長しない。
- 期限に達した側は、送信済みなら Alt up を送るか、送れない場合に備えて解放待ちにする。
- その側の「押されている」という内部記録は破棄する。無変換／変換は送らない。
- まだ Alt down を送っていなかった場合は、期限切れ後に古い Alt down を新たに送らない。
- 期限切れ後に古い物理 up が来ても、無変換／変換を発生させない。
- その後の新しい押下は通常どおり処理できること。

物理的に本当に5秒以上押し続ける操作は利用前提外である。超過後の連続リピートから新規押下を完全に識別する機能を追加して複雑化する必要はない。ただし、保持中のリピートで元の期限を延長してはならない。

### 5.2 タイマーが遅れた場合

- 期限は「タイマーが何回呼ばれたか」で数えず、時刻差から計算する。
- 入力イベント処理時にも、古い Alt 状態を使う前に期限を確認する。
- 判定の優先順位は **5秒の期限切れ → 150 msの長押し判定**。スリープや停止から復帰し、両期限を過ぎている場合は古い down を送らず破棄する。
- 5秒はリアルタイムOSの保証ではない。プロセスが停止・スリープしている間の処理実行は保証せず、実行再開時に期限を適用する。

### 5.3 ロック専用処理は入れない

ユーザーは明示的に、**ロック／解除に応じた特別な処理を削除する**よう求めている。

- ロック通知の登録、解除時リセット、ロック中フラグなどを既定設計として再導入しない。
- 例として `WTSRegisterSessionNotification`／`WM_WTSSESSION_CHANGE` による専用処理は不要。
- Win/L 等の全キー履歴を持たないことと、Alt の5秒制限で対応する。
- ロック解除時に即座に全状態をリセットする保証は不要。5秒未満で復帰した場合、まだ期限前の Alt 状態が残る可能性は許容する。

### 5.4 入力送信が失敗した場合

- 入力送信APIの成功数を確認する。成功したことにして黙って送信済み状態を残さない。
- 5秒制限による解放を送れなくても、押下記録を復活させない。**Alt up のみを再試行する小さな解放待ち状態**は持ってよい。これは他キーの押下一覧ではない。
- 解放待ちは左右 Alt に限定し、無制限にイベントを蓄積しない。送信できるようになったら解放する。
- 解放待ちを正常に送信できるまでは、新しい変換処理を進めず物理入力を通す等、キーを無期限に飲み込まない動作とする。
- タイマーからの Alt down が失敗／部分成功した場合も、その押下の管理をリセットし、注入された可能性がある Alt を解放する。タイマーの送信失敗だけで常駐を終了させない。
- 通常の組み合わせ入力の送信失敗は、安全に変換を停止し、後片付けしてユーザーに通知する方式でよい。元の複合入力を無条件に丸ごと再送すると二重入力になり得るため避ける。
- エラー表示や待機をキーボードフックのコールバック内で行わない。

## 6. 常駐アプリとして必要な動作

- exe を起動すると入力監視が開始し、通常はバックグラウンドで常駐する。
- 終了手段を用意する。通知領域アイコンと「終了」メニューでよい。大きな設定画面は不要。
- 同じツールを二重起動して入力を重複処理しない。既存プロセスを維持して二重起動を止める方式、または安全に旧プロセスを終了して置き換える方式のどちらでもよい。
- 通常終了時はタイマー／変換を停止し、**自分が送信済みの Alt down に対応する up と解放待ち up を可能な範囲で送る**。フックなどのリソースを解放する。
- Alt を押している途中の正常終了でも、押しっぱなしを残さないようにする。
- 強制終了、OS停止、入力注入が拒否される環境まで完全な解放を保証する必要はない。
- OS起動時の自動登録、レジストリ変更、自動昇格、サービス化は要求しない。
- 開発環境からの起動だけでなく、**配布用に発行した exe が実際に常駐を開始すること**を検証する。

## 7. 状態モデルの参考

以下は実装の推奨モデルであり、クラス名・フィールド名を固定するものではない。

各 Alt は概念的に次の3状態を持つ。

| 状態 | 意味 |
| --- | --- |
| Idle | 有効な押下記録なし |
| Pending | down を捕捉したが、まだ Alt down を送っていない |
| Active | 修飾キーとして有効化し、Alt down を送信した／送信を管理している |

保持する追加情報は、最初の押下時刻と、解放が必要な送信状態などに限定する。コールバックが観測した「down の記録」は、現在も物理的に押されていることの確実な証拠ではない。up は取り逃す可能性がある。

| 入力・条件 | 遷移と出力 |
| --- | --- |
| Idle で新規 Alt down、先押し修飾キーなし・反対 Alt なし | Pending。出力なし |
| Idle で新規 Alt down、先押し修飾キーあり | Active。対応 Alt down |
| Pending で150 ms未満の up | Idle。無変換または変換の down/up |
| Pending で150 ms経過、まだ5秒未満 | Active。対応 Alt down |
| Pending 中に他キー down | Active。Alt down を先に送ってから相手キー down |
| Pending 中に反対 Alt down | 両側 Active。必要な両 Alt down |
| Active の up、まだ5秒未満 | Idle。対応 Alt up |
| 有効な押下記録が5秒に到達 | Idle。必要な Alt up。送れなければ解放待ち |
| Idle の up | タップを作らない。通常はその up を通す |

タイマーが遅れて Pending のまま150 ms以上の up を受けた場合の処理は、4.3節を参照。

**不変条件：**

- 短い単独タップに Alt down が混入しない。
- 相手キーを1回押したら、相手アプリには原則1回分だけ届く。
- 同一押下の開始時刻はリピートで変化しない。
- 合成入力を新しい物理入力として再処理しない。
- 期限切れの押下記録は通常の入力処理に持ち越さない。
- 左右 Alt の一方の解放・期限切れで、もう一方の有効な押下を不用意に壊さない。

## 8. C#／Windows API の推奨設計と注意点

### 8.1 入力処理方式

現在の動作を実現している方式は、低レベルキーボードフックで物理 Alt を抑止し、必要なイベントを Win32 API で注入する構成である。C# でもこの方式が有力。ただしAPIを呼ぶだけで Alt+Tab が必ず動くと考えず、実機で確認すること。

- フック：`SetWindowsHookExW`、`WH_KEYBOARD_LL = 13`。
- フックデータ：`KBDLLHOOKSTRUCT`。通常キーの down/up と system-key の down/up の両方を扱う。
- 注入：`SendInput` と `INPUT`／`KEYBDINPUT`。
- 先押し修飾キーの状態取得：`GetAsyncKeyState` の上位ビット。
- 未処理イベントの転送：`CallNextHookEx`。負のフックコードはそのまま転送する。

判定待ち中の相手キーを扱うときは、必要な Alt down と相手キー down を一つの入力配列に入れ、順序を保って送る方法を検討する。その元イベントは抑止する。元イベントを通したうえで後から Alt down を送ると、相手キーに Alt が付かない可能性がある。

### 8.2 合成入力とフラグ

- `LLKHF_INJECTED` 等を使い、合成イベントを物理入力の状態管理から除外する。
- 自分の送信には `dwExtraInfo` に識別子を付ける方式を推奨する。他アプリの合成イベントも物理押下の履歴としては扱わず、そのまま通す。
- 左 Alt と右 Alt を保持する。右 Alt の extended-key フラグを落とさない。
- 相手キーを再送する場合、virtual-key・scan code・extended-key の情報を適切に保持する。低レベルフックの flags 全体を `KEYBDINPUT.dwFlags` にそのままコピーしない。両構造体のビット定義は異なる。
- 全イベントを文字列に変換して再送する実装は避ける。修飾キー、非文字キー、キーリピート、配列依存の問題がある。

先押し状態照会では `VK_SHIFT = 0x10`、`VK_CONTROL = 0x11`、`VK_LWIN = 0x5B`、`VK_RWIN = 0x5C` が使える。低レベルフックは当該イベントの非同期キー状態が更新される前に呼ばれるため、フック対象の Alt 自身を `GetAsyncKeyState` だけで判定しない。注入した Alt の論理状態と物理状態も混同しない。[GetAsyncKeyState](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getasynckeystate)、[LowLevelKeyboardProc](https://learn.microsoft.com/en-us/windows/win32/winmsg/lowlevelkeyboardproc)

### 8.3 スレッド・寿命・時刻

- フックを登録したスレッドにメッセージループを持たせる。
- キー入力・タイマー・終了処理による状態変更を直列化する。スレッドプールのタイマーから同じ状態を無同期で更新しない。
- `SendInput` 等からコールバックが再入し得ることを考慮し、自分の合成イベントの除外と状態更新順序を設計する。
- フック内では待機、重いI/O、ダイアログ表示などを行わない。Windows はフックがタイムアウトすると通知なく解除する場合がある。[Microsoft の説明](https://learn.microsoft.com/en-us/windows/win32/winmsg/lowlevelkeyboardproc)
- delegate をコールバックに使う場合は、登録中に GC で回収されないよう参照を保持する。アンマネージド側へ例外を漏らさない。関数ポインター等を使う場合も寿命と呼出規約を管理する。
- 時間測定は単調な時刻源を使う。タイマーと入力イベントの経過時間を同じ時間軸で比較する。壁時計の日時変更でタップ・期限を変化させない。
- 32 bit のイベント時刻を使う場合は桁あふれを扱う。64 bit の時刻と無変換で引き算しない。
- タイマー間隔は例として10 ms程度でよいが、正確に10 msで呼ばれることを前提にしない。

### 8.4 x86／x64 のデータ配置

`INPUT` はキーボード専用構造体ではなく union を含む。C# でキーボード用フィールドだけを小さく定義するとサイズが不足し、送信失敗の原因になる。

| 確認項目 | x86 | x64 |
| --- | --- | --- |
| `sizeof(INPUT)` | 28 bytes | 40 bytes |
| `INPUT` 内の union 開始位置 | 4 | 8 |
| `KEYBDINPUT.dwExtraInfo` の KEYBDINPUT 内 offset | 12 | 16 |
| `sizeof(KBDLLHOOKSTRUCT)` | 20 bytes | 24 bytes |
| `KBDLLHOOKSTRUCT.dwExtraInfo` の offset | 16 | 16 |

ポインターサイズに依存する値は `nint`／`nuint` 等を適切に使う。マーシャリング後のサイズ・offsetをテストする。`SendInput` の `cbSize` には正しい `INPUT` サイズを渡す。[INPUT](https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-input)、[KBDLLHOOKSTRUCT](https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-kbdllhookstruct)、[.NET の相互運用指針](https://learn.microsoft.com/en-us/dotnet/standard/native-interop/best-practices)

### 8.5 権限と失敗の扱い

`SendInput` は自分より高い整合性レベルの対象には注入できない場合がある。戻り値が0でも UIPI が原因であることをエラーコードだけから必ず特定できるわけではない。[Microsoft の説明](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-sendinput)

権限制限やフックのタイムアウトは、C# に変更するだけではなくならない。必要な対応範囲と制約を README に記載し、セキュリティ設定の回避を実装目的にしない。

## 9. 受け入れテスト

### 9.1 自動テスト

時計・修飾キーの現在状態・入力送信結果を差し替えられる設計を推奨する。テスト自体が実ユーザーのウィンドウを切り替えたり閉じたりしないよう、状態テストと実機テストを分離する。

| ID | ケース | 期待結果 |
| --- | --- | --- |
| A01 | 左 Alt を30 msで単独解放 | 無変換 down/up のみ |
| A02 | 右 Alt を30 msで単独解放 | 変換 down/up のみ |
| A03 | 99 msで単独解放 | タップ |
| A04 | 150 msで解放 | 長押し扱い。無変換／変換なし |
| A05 | 150 ms到達後も保持 | Alt down は1回、解放時に対応 up |
| A06 | Alt down→10 ms後にTab down | Alt down→Tab down の順。二重Tabなし |
| A07 | Alt+Shift+Tab、Shiftの先押し／後押し | 修飾順序が正しく、IMEキーが出ない |
| A08 | Shift・Ctrl・左右Winの先押し | 現在状態の照会で即時Alt有効化 |
| A09 | 過去にShift downがあったが現在状態は非押下、up履歴なし | 過去の通知に依存せずタップできる |
| A10 | Win/L downのみ受信し、そのupを欠落させる | 現在の修飾状態が非押下なら、その後のタップが動く |
| A11 | 左右Altを重ね、押す順・離す順を全組み合わせ | 状態が残らず、その後の左右タップも動く |
| A12 | 片側Altのup欠落、他方を繰り返し操作 | 欠落側は元のdownから5秒で破棄。他方の操作で期限延長なし |
| A13 | 両側Altのup欠落 | 各側が独立した5秒期限で破棄され、その後のタップが復活 |
| A14 | 左downを時刻0、右downを1000 msに発生 | 左は5000 ms、右は6000 msで期限切れ |
| A15 | AltリピートやTabを繰り返す | 最初のAlt downからの5秒期限を維持 |
| A16 | 4999 ms／5000 msの境界 | 前者では有効、後者では期限切れ |
| A17 | 判定待ちのままタイマーが5秒超遅延 | 古いAlt downやタップを生成しない |
| A18 | タイマーが動く前に期限後の入力が来る | 入力処理の冒頭で期限切れを適用 |
| A19 | 期限切れ後に古いAlt up | 無変換／変換を送らない |
| A20 | Alt upの送信失敗→後に成功 | 押下記録は破棄済み。upのみ再試行し、正常復帰 |
| A21 | タイマーによるAlt downの送信失敗・部分成功 | 常駐を継続して状態をリセットし、必要な解放を行う |
| A22 | 自分・他アプリの合成入力 | 物理押下として再処理しない。再帰や重複なし |
| A23 | Altの保持中／解放待ち中に通常終了 | 対応する解放とリソース後片付けを試みる |
| A24 | x86／x64 の構造体配置 | サイズ・offsetとポインター幅が正しい |
| A25 | 時刻の桁あふれ・壁時計変更 | 経過時間の判定が壊れない |
| A26 | ランダムな入力・タイマー・up欠落を多数回反復 | タップ条件とAltのdown/up対応、期限の不変条件を保つ |

送信失敗を含むテストでは「内部的に up を作った」だけで成功とせず、未送信の解放待ちと実際に受理された入力を分けて検証する。

### 9.2 Windows 実機テスト

自動テストだけでは Alt+Tab、IME、フックの競合や配布exeの起動を保証できない。少なくとも以下を別途確認する。

1. Alt downを記録できるテスト用アプリ等で、短い単独タップに Alt が漏れないこと。
2. 日本語入力環境で、左右の無変換／変換キーイベントが期待どおり届くこと。
3. 素早い左右 Alt+Tab と Alt+Shift+Tab、Tabの複数回押し、Alt解放による確定。
4. Shift・Ctrl・Winの先押しを含むショートカットと、通常のAltショートカット。
5. 左右Altの重なりを繰り返した後、タップが正常に動くこと。
6. Win+L→ロック解除後も動くこと。ロック専用処理を実装し直すのではなく、現行の設計で確認する。
7. 時間経過やフォーカス切替後も常駐が継続すること。
8. 配布用exeの起動・終了・二重起動対策。開発用ランナーだけで確認を済ませない。
9. x86／x64の確認状況を分けて記録する。実行が環境ポリシーで阻まれた場合は未確認と明記する。

画面ロックや終了操作を伴うテストは、ユーザーの作業を妨げないタイミングで行う。実施できなかった項目を「確認済み」と扱わない。

## 10. これまでの経緯・再導入しない方式

| 方式・問題 | 経緯と教訓 |
| --- | --- |
| 物理Altを通し、タップ時にIMEキーを追加 | Alt downに反応するアプリの問題が解決しなかった。単独タップ中の物理Altは抑止が必要 |
| Escでメニューを解除 | 目的外のEsc入力が発生した。採用しない |
| AHK InputHookで次キーを捕捉しAltと再送 | ユーザーの試行ではAlt+Tab等が動かなかった。これは試した方式の結果であり、すべての注入方式が不可能という意味ではない |
| タイマーだけでAlt downを送り、up管理が別系統 | 物理upと合成upが競合し、押しっぱなしが発生した。状態変更・送信・解放を一貫して管理する |
| Alt以外の全キーの押下履歴 | upを取り逃すと、その後の全Altタップが組み合わせ扱いになった。ユーザーがWin+Lで再現を確認。現行仕様ではこの履歴を廃止 |
| ロック／解除通知でのリセット | 一度導入し、ユーザーがロック後の症状改善を確認。その後、ユーザーの明示指示で削除。5秒制限と履歴廃止に置き換えた |
| 左右Alt同時押し後の不調 | ユーザーが関連を疑った。通常のdown/upがそろう状態テストでは再現せず、片側up欠落を与えると同様の症状が出た。実際に同時押しが通知欠落を起こす原因かは未確定 |
| exe起動時に何も始まらない | AHK特有のファイルパス比較がexe化後に成立せず、初期化を飛ばしていた。32/64 bitの問題ではなかった。C#版では配布exeの起動経路を実行確認する |
| タップ閾値200 ms | 旧値。現行値は150 ms |

既存版の初期段階ではユーザーが正常動作を確認しているが、その後に上記の不具合と仕様変更があった。**最新版の5秒制限版は状態テストを通した段階であり、長時間の実機安定性が確認済みという意味ではない。**

直近のAHK版ではx86／x64のスクリプトの状態・初期化テストと、x64のコンパイル済みテスト版の起動等を確認した。x86のexe実行確認は環境側の制限で未完了だった。これはC#版の検証を代替しない。

## 11. 納品物と自由に決めてよい事項

必要な成果物：

1. AHKに依存しないC#のソース一式とプロジェクト定義。
2. ビルド・テスト・発行の再現手順。
3. x86／x64の発行方法。作成した実行ファイルはアーキテクチャを識別できるようにする。
4. 状態管理・5秒制限・入力注入の境界を検証する自動テスト。
5. 起動・終了・時間値の変更方法、権限等の制約を記したREADME。
6. 実施したテストと未実施のテストを区別した検証結果。

実装者が判断してよい事項：

- .NETの具体的なバージョン、WinForms等の使用有無、ソース生成によるP/Invokeの採用。
- 設定値を定数にするか簡単な設定ファイルにするか。
- ランタイム同梱の有無、単一ファイル発行の有無。ユーザーが起動に必要なものを把握できるよう明記する。
- 内部クラス構成、テストフレームワーク、ログや診断の最小限の仕組み。

追加要求ではないもの：設定GUI、自動更新、自動スタートアップ登録、サービス化、IME状態管理、汎用キーリマップ、ロック専用処理。必要以上に機能を増やさず、この資料の動作を安定して実現することを優先する。

## 12. 参考資料

以下は実装上の確認先であり、本資料の要件を読むための必須前提ではない。

- [LowLevelKeyboardProc — フックの呼出し、メッセージループ、タイムアウト](https://learn.microsoft.com/en-us/windows/win32/winmsg/lowlevelkeyboardproc)
- [SendInput — 入力順序、戻り値、UIPI](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-sendinput)
- [GetAsyncKeyState — 修飾キーの現在状態](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getasynckeystate)
- [INPUT](https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-input)
- [KEYBDINPUT](https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-keybdinput)
- [KBDLLHOOKSTRUCT](https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-kbdllhookstruct)
- [.NET のネイティブ相互運用指針](https://learn.microsoft.com/en-us/dotnet/standard/native-interop/best-practices)
